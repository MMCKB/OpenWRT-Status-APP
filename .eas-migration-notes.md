# Expo EAS 项目关联说明

- Expo 官方 CI 文档说明：`EXPO_TOKEN` 用于认证 EAS CLI；首次配置会生成并写入 `extra.eas.projectId`，以关联本地项目和 EAS 远端项目。
- 当前远端项目已在个人账号 `mmckbzn` 下创建，地址为 `https://expo.dev/accounts/mmckbzn/projects/openwrt-status-app`；EAS CLI 在将项目 ID 写回动态 `app.config.ts` 时失败。
- 下一步需要获取该远端项目的 ID，并手动将其写入 `app.config.ts` 的 `extra.eas.projectId`；之后再以新账号令牌验证归属。

来源：

- https://docs.expo.dev/build/building-on-ci/
- https://docs.expo.dev/eas/cli/
