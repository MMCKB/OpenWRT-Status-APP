# OpenWrt 路由器状态：移动端界面设计

## 设计目标

本应用面向需要在局域网内快速检查 OpenWrt 路由器健康状况的用户。界面以 **9:16 竖屏** 与单手操作为前提，采用接近 iOS 系统设置与家庭应用的层级：用户首先看见当前选中路由器是否在线，然后通过清晰的分组卡片了解系统负载、内存、存储、网络接口与无线状态。应用不会捏造设备数据；在尚未连接路由器时，将明确显示配置引导。

## 信息架构与屏幕清单

| 屏幕 | 主要内容 | 可执行操作 |
|---|---|---|
| 状态 | 在线状态、最近刷新时间、运行时间、CPU 负载、内存与存储、接口及 Wi-Fi 概览 | 下拉刷新、进入路由器选择、打开详情分区 |
| 路由器 | 已保存的路由器列表、地址、当前选中状态、健康状态 | 选择、编辑、删除、添加路由器 |
| 添加/编辑路由器 | 名称、管理地址、用户名、密码、连接提示与验证状态 | 测试连接、保存、删除 |
| 设备详情 | 系统、网络与无线分区的完整状态字段 | 切换分区、手动刷新 |
| 设置 | 自动刷新频率、深色外观说明、隐私与连接说明 | 修改自动刷新频率 |
| 实时流量排行 | 在线设备的即时上行、下行与合计速率，按合计速率降序展示 | 刷新排行、进入设备管理并执行已有拉黑操作 |
| DHCP 与静态租约 | 动态租约、静态租约、MAC、IPv4、主机名及租约期限 | 新增、编辑、删除静态租约；在确认后应用 DHCP 配置 |
| 无线优化助手 | 无线设备、当前信道、扫描到的邻近网络、信道拥挤度与建议 | 扫描、查看建议、确认后修改指定无线设备信道 |

## 核心用户流程

| 场景 | 用户路径 | 结果 |
|---|---|---|
| 首次连接 | 状态空白页 → 添加路由器 → 输入 LuCI 地址与账户 → 测试连接 → 保存 | 成功后跳转状态页并拉取真实状态 |
| 查看状态 | 状态页 → 下拉刷新或点击刷新 → 系统卡片/接口卡片 | 数据来源、时间及错误状态可追溯 |
| 管理多台设备 | 路由器页 → 选择另一设备 → 状态页刷新 | 当前设备在页面顶部明确显示 |
| 修改连接 | 路由器页 → 编辑 → 保存或删除 | 配置与密码本地更新，状态缓存失效 |
| 识别高流量设备 | 工具 → 实时流量排行 → 刷新 | 从路由器可用的主机级流量计数器读取速率；不支持时明确展示原因，不伪造数据 |
| 固定设备 IP | 工具 → DHCP 与静态租约 → 新增静态租约 → 输入名称、MAC 与 IPv4 → 确认 | 应用写入受控 UCI `dhcp` 配置并重载 dnsmasq，随后重新读取结果 |
| 优化 Wi-Fi 信道 | 工具 → 无线优化助手 → 扫描 → 查看建议 → 选择建议并确认 | 仅修改所选 radio 的信道，提交前展示影响与重载提示 |

## 布局规范

状态页顶部使用左对齐的设备名称与一枚在线状态圆点，右侧只保留刷新图标，确保可触区域至少为 44pt。关键健康信号以两列数据卡呈现，数值以等宽字体显示以便快速扫读。网络与无线摘要采用全宽列表卡，点击后进入详情而不是在主屏堆叠过多字段。底部使用四个标签：状态、路由器、详情、设置；未配置设备时隐藏不可用详情内容并展示引导。

新增的三个管理页继续采用单列纵向滚动与顶部刷新动作。流量排行首屏最多突出前三台设备，其余设备置于等高列表，以减少拥挤；若路由器缺少主机级流量计数能力，页面以信息卡解释限制，并提供跳转到现有设备管理页的入口。DHCP 页面将“动态租约”和“固定地址”分开呈现，编辑操作通过底部表单或系统对话框完成，删除始终需要确认。无线优化页先显示只读扫描结果和保守建议；任何会使无线短暂重载的信道变更均为第二步确认操作，且明确列出目标 radio、当前信道和建议信道。

## 色彩与排版

品牌主色采用 **OpenWrt 青绿 `#007E7A`**，表达网络可用与系统正常；深海军蓝 `#102A43` 用于主文字与深色背景；雾白 `#F6F8FA` 用于页面底色；成功色为 `#1B9A6A`，警告色为 `#C77A00`，错误色为 `#C53B3B`。所有状态颜色均同时配合文字与图标，不只依赖颜色传递意义。标题使用高对比度粗体，次要时间和技术标签使用较小的灰蓝色文字，遵循 iOS 可读性和信息层级。

## 本地数据模型

| 模型 | 字段 | 存储策略 |
|---|---|---|
| RouterProfile | id、name、baseUrl、username、createdAt、lastConnectedAt | AsyncStorage |
| RouterSecret | profileId、password | SecureStore；密码不进入普通配置存储 |
| RouterStatus | routerId、fetchedAt、online、system、memory、storage、interfaces、wireless、error | 仅内存缓存，连接失败时显示错误而不伪造数据 |
| AppSettings | refreshIntervalSeconds、selectedRouterId | AsyncStorage |
| HostTrafficRate | MAC、IPv4、主机名、上行/下行字节计数、采样时间 | 仅内存采样；每次重新读取时计算差分，不写入路由器 |
| DhcpLease | 来源、MAC、IPv4、主机名、到期时间、UCI 区段 | 动态租约仅内存；静态租约由路由器 `/etc/config/dhcp` 作为唯一事实来源 |
| WirelessOptimization | radio、当前信道、扫描项、拥挤度、建议 | 仅内存；确认后通过受控 UCI 写入 |

## 可访问性与反馈

交互元素均使用明确的无障碍标签；刷新和保存操作提供加载状态，连接错误以可读文本呈现。危险操作（删除路由器）需要原生确认对话框。网络状态仅在用户主动保存并连接的地址上读取；账户密码存储于设备安全密钥链或密钥库。
