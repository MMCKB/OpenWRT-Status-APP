# EAS 直接构建依据

- Expo 官方说明：`eas.json` 位于项目根目录，首次使用 `eas build:configure` 时生成；配置位于 `build` 键下，构建可按指定 profile 执行。
- Expo 官方 Android APK 文档说明：Android 安装包可通过 build profile 的 `android.buildType: "apk"` 生成；使用 `eas build -p android --profile preview` 发起构建，构建完成后可从构建详情页或 CLI 提供的链接下载 APK。
- 本项目配置的 `preview` profile 采用 `distribution: "internal"` 与 `android.buildType: "apk"`，用于真机安装测试；`production` profile 默认输出 AAB，适合未来 Google Play 发布。

来源：

1. https://docs.expo.dev/build/eas-json/
2. https://docs.expo.dev/build-reference/apk/
3. https://docs.expo.dev/build/introduction/
