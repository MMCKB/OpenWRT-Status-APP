# OpenWrt 25.12 apk 适配资料

本轮外部资料：

- OpenWrt 官方 apk 文档：https://openwrt.org/docs/guide-user/additional-software/apk
  - 搜索结果确认 apk 是 OpenWrt 25.12 及更新版本使用的包管理器。
  - 搜索结果摘要指出 `upgrade` 用于查找在线仓库中的软件包更新并升级。
- OpenWrt 官方软件包页面：https://openwrt.org/packages/start
  - 搜索结果摘要说明需要先执行 Update lists 获取可用软件包列表，再使用 Filter 搜索。
- Alpine apk 文档：https://wiki.alpinelinux.org/wiki/Alpine_Package_Keeper
  - 搜索结果摘要说明 `apk update` 用于获取最新仓库索引。
- Alpine apk 可更新列表参考：https://kvibber.com/tech-tips/apk-count-upgrades/
  - 搜索结果摘要说明 `apk list -u` 可列出可升级软件包。
- Alpine 手册参考：https://docs.alpinelinux.org/user-handbook/0.1a/Working/apk.html
  - 搜索结果摘要说明系统升级通常由 `apk update` 后执行 `apk upgrade` 完成。

实现结论：OpenWrt 25.12 软件包页面应使用 `apk update`、`apk info -v`、`apk list -u`、`apk search -v`、`apk add`、`apk del` 和 `apk upgrade`；不能再显示或执行 opkg update/list/search/install/remove。官方页面正文受 Anubis 反爬挑战保护，未直接提取到正文，因此命令实现以官方搜索摘要和 apk 生态文档交叉核对为依据。
补充资料：

- Alpine APK 官方手册：https://wiki.alpinelinux.org/wiki/Alpine_Package_Keeper
  正文说明 `apk update` 下载并缓存 APKINDEX；`apk upgrade` 可升级全部 World 包；指定包名可执行单包升级；`apk -U upgrade` 等价于先更新索引再升级。
- Alpine 用户手册：https://docs.alpinelinux.org/user-handbook/0.1a/Working/apk.html
  正文说明 `apk search` 用于仓库搜索，`apk add` 安装精确包名，`apk del` 卸载，`apk update` 后再 `apk upgrade` 完成系统升级；`apk info` 用于查询包信息。
- apk-query 手册：https://man.archlinux.org/man/apk-query.8.en
  正文列出 `--upgradable` 查询选项，说明 apk query 可使用 name、version、description、arch 等字段；如果后续需要更稳定的机器解析，可考虑 `apk query --upgradable --format json`，但当前 OpenWrt 25.12 兼容性仍需在真实路由器上验证。
